// React code can be written without JSX -- every JSX element is a call to React.createElement()

// it takes three args: the element, a property, and the content

// http://facebook.github.io/react/docs/top-level-api.html#react.createelement

// Probably most people use JSX though.
