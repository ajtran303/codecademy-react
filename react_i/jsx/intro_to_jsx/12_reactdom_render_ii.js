// The first argument to ReactDOM.render() is appended to whatever elements are selected by the second argument.

// That selected element acts as a container for the first argument (a JSX element)

import React from 'react';
import ReactDOM from 'react-dom';

// Write code here:
ReactDOM.render(<h1>Render me!</h1>, document.getElementById('container'));
