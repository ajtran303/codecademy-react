// JSX elements can be nested inside of each other.

// Nested JSX expressions are pretty much treated as regular JS expressions.

// They can be saved to a variable, etc...

// Use pretty print to make it pretty

// Multiline JSX expressions must be wrapped in parentheses.

let myDiv = (
  <div>
    <h1>
      Hello world
    </h1>
  </div>
);
