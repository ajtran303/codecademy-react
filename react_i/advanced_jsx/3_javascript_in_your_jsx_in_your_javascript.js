// Any code inside of JSX is.. JSX.

// In this example below, 2 + 3 is rendered to the screen as `2 + 3` and not evaluated as a JS expression


import React from 'react';
import ReactDOM from 'react-dom';

// Write code here:
ReactDOM.render(
  <h1>2 + 3</h1>,
  document.getElementById('app')
);
