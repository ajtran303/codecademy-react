// import ReactDOM from 'react-dom';

// this library contains methods to deal with the DOM eg. ReactDOM.render()

// vs methods from `react` are only for React things ie. components and JSX, etc

import React from 'react';
import ReactDOM from 'react-dom';
