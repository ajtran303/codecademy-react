// A component can change its own state with `this.setState()`

// It takes two args: an obj to update state and an optional callback function

// That object is then merged with the component's current state after diffing